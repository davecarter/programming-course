const isMultipleOfFive = (n) => n % 5 === 0;

const getMultiplesOfFive = function(a, b) {
  for (var i = a; i <= b; i++) {
    console.log('retorna rango de números entre a y b');
  };
}

export {isMultipleOfFive, getMultiplesOfFive};